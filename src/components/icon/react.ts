import {createComponent} from '@lit-labs/react';
import * as React from 'react';
import {HyIconElement} from './icon.component';
export const HyIcon = createComponent({
  tagName: 'hy-icon',
  elementClass: HyIconElement,
  react: React,
});
